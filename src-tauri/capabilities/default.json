{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core permissions for the main window. Only the event API is granted: Rust→JS push (content updates, progress) rides on emit/listen, and SPIKE-002 showed that with no capability file at all, listen() is denied and every event silently vanishes. Nothing else from core is granted until something needs it.",
  "windows": ["main"],
  "permissions": ["core:event:default"]
}
